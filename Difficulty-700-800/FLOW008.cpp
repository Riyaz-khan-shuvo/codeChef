#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n;
        scanf("%d", &n);
        n < 10 ? printf("Thanks for helping Chef!\n") : printf("%d\n", -1);
    }

    return 0;
}